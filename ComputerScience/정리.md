# 정리

## 바로가기
- [동일성과 동등성](#동일성과-동등성)
- [객체지향](#객체지향)
- [ArrayList의 크기의 변화](#arraylist의-크기의-변화)
- [오버라이딩](#오버라이딩)
- [오버로딩](#오버로딩)
- [프로토콜](#프로토콜)
- [웹 브라우저에서 www.google.com에 접속하면 어떤 일이 생길까](#웹-브라우저에서-wwwgooglecom에-접속하면-어떤-일이-생길까)
- [웹 브라우저에서 URL을 처리하는 과정을 설명, URL이 www.google.com/search?q=url과 같이 되어 있다면?](#웹-브라우저에서-url을-처리하는-과정을-설명-url이-wwwgooglecomsearchqurl과-같이-되어-있다면)
- [만약 DNS가 www.google.com 이라는 도메인에 대한 정보를 가지고 있지 않다면?](#만약-dns가-wwwgooglecom-이라는-도메인에-대한-정보를-가지고-있지-않다면)
- [트래픽은 사용자의 PC에서 구체적으로 어떤 과정을 거쳐 빠져나가는지 OSI 7계층이나 TCP/IP 4계층을 기준으로 설명](#트래픽은-사용자의-pc에서-구체적으로-어떤-과정을-거쳐-빠져나가는지-osi-7계층이나-tcpip-4계층을-기준으로-설명)
- [라우터는 어떻게 구글 웹 서버를 찾아가나요?](#라우터는-어떻게-구글-웹-서버를-찾아가나요)
- [구글 웹 서버 장비에서 어떻게 트래픽이 웹 서비스로 접근하나요?](#구글-웹-서버-장비에서-어떻게-트래픽이-웹-서비스로-접근하나요)
- [사용자의 웹 브라우저에 HTML이 렌더링되는 것 외에 다른 과정이 또 있을까요?](#사용자의-웹-브라우저에-html이-렌더링되는-것-외에-다른-과정이-또-있을까요)
- [TCP와 UDP의 차이](#tcp와-udp의-차이)
- [공인 IP와 사설 IP의 차이](#공인-ip와-사설-ip의-차이)
- [로드 밸런싱](#로드-밸런싱)
- [스프링 프레임워크와 스프링 부트의 차이](#스프링-프레임워크와-스프링-부트의-차이)
- [객체지향적인 개발이 어떤 것인지 설명](#객체지향적인-개발이-어떤-것인지-설명)
- [의존성 주입](#의존성-주입)
- [제어의 역전](#제어의-역전)
- [라이브러리와 프레임워크 차이](#라이브러리와-프레임워크-차이)
- [maven과 gradle 차이](#maven과-gradle-차이)
- [URL과 URI의 차이](#url과-uri의-차이)
- [웹 브라우저가 웹 서버와 상호작용 할 수 있는 방법에는 어떤 것들이 있나](#웹-브라우저가-웹-서버와-상호작용-할-수-있는-방법에는-어떤-것들이-있나)
- [웹 소켓](#웹-소켓)
- [컴파일 언어와 인터프리터 언어](#컴파일-언어와-인터프리터-언어)
- [JSON과 xml의 차이](#json과-xml의-차이)
- [AJAX가 필요한 이유와 AJAX를 사용하면 무엇을 할 수 있나요?](#ajax가-필요한-이유와-ajax를-사용하면-무엇을-할-수-있나요)
- [동기와 비동기](#동기와-비동기)
- [서로 다른 애플리케이션끼리 통신할 때 더 효율적인 프로토콜을 사용할 수 있는데, 왜 아직 HTTP를 많이 사용하는 건가요?](#서로-다른-애플리케이션끼리-통신할-때-더-효율적인-프로토콜을-사용할-수-있는데-왜-아직-http를-많이-사용하는-건가요)
- [HTTP/3에서는 왜 TCP가 아니라 UDP를 사용하나요?](#http3에서는-왜-tcp가-아니라-udp를-사용하나요)
- [HTTP 메서드 중 GET과 POST는 각각 어떤 기준으로 구분해 사용하나요?](#http-메서드-중-get과-post는-각각-어떤-기준으로-구분해-사용하나요)
- [멱등성이란](#멱등성이란)
- [Content Negotiation](#content-negotiation)
- [HTTP와 HTTPS의 차이는 무엇인가요?](#http와-https의-차이는-무엇인가요)
- [어떤 자원을 저장할 때는 숫자로 된 id를 저장하곤 합니다. 만약 id가 엄청 커진다면 어떤 자료형을 사용하는 것이 적절할까요?](#어떤-자원을-저장할-때는-숫자로-된-id를-저장하곤-합니다-만약-id가-엄청-커진다면-어떤-자료형을-사용하는-것이-적절할까요)
- [레이더드 아키텍처에 대해 설명](#레이더드-아키텍처에-대해-설명)
- [스레드 safe하다 라는 말의 의미](#스레드-safe하다-라는-말의-의미)
- [애플리케이션을 개발할 때 DTO를 만들어 사용하는 이유](#애플리케이션을-개발할-때-dto를-만들어-사용하는-이유)
- [스프링 프레임워크에서는 왜 Bean을 만들고 주입하여 사용할까?](#스프링-프레임워크에서는-왜-bean을-만들고-주입하여-사용할까)
- [웹 애플리케이션을 개발하면서 데이터를 애플리케이션 위에 저장해도 서비스가 가능할 것 같은데, 왜 데이터베이스를 사용하는지?](#웹-애플리케이션을-개발하면서-데이터를-애플리케이션-위에-저장해도-서비스가-가능할-것-같은데-왜-데이터베이스를-사용하는지)
- [트랜잭션이란](#트랜잭션이란)
- [DB 커넥션 풀은 왜 사용하나요?](#db-커넥션-풀은-왜-사용하나요)
- [Thread Pool](#thread-pool)
- [어떤 DB를 사용해 봤나요?](#어떤-db를-사용해-봤나요)
- [데이터베이스 비밀번호를 application.properties에 올려두면?](#데이터베이스-비밀번호를-applicationproperties에-올려두면)
- [객체지향 프로그래밍에서 이야하는 추상화란?](#객체지향-프로그래밍에서-이야하는-추상화란)
- [리팩토링이란?](#리팩토링이란)
- [통합 테스트와 단위 테스트 차이](#통합-테스트와-단위-테스트-차이)

## [동일성과 동등성](#)
동일성은 '두 인스턴스가 같은 존재인지 판단'하는 것  
자바에서는 `==` 연산자로 두 인스턴스의 레퍼런스 변수를 비교함  
비교의 결과가 true라면 동일한 인스턴스  
동등성은 '두 인스턴스가 같은 값으로 평가되는지 확인'하는 것
String 클래스는 자바의 `equals()` 메서드를 통해 확인하고, 직접 선언한 클래스는 equals() 메서드를 오버라이딩해 어떤 조건에서 동등하다고 판단할지 정해줘야 함

## [객체지향](#)
객체지향의 핵심은 `추상화`와 `다형성`  
추상화와 다형성은 인터페이스로 연결되는데  
인터페이스 없이 개발을 하면 클라이언트 코드가 구체적인 클래스에 의존하게 되고, 하나의 클래스가 변경되면 다른 클래스도 변경해줘야 하는 문제가 생긴다.  
이런 문제를 해결하려면 클라이언트 코드가 구체적인 클래스가 아니라 인터페이스에 의존해야한다.  
결국 클라이언트 코드와 구체적인 클래스 모두 인터페이스에 의존하게 된다.  

몇 가지 이점으로 첫 번째. `클라이언트 코드는 해당 인터페이스를 구현하고 있는 구체적인 클래스의 세부 구현을 몰라도 된다.`  
인터페이스에 의존하고 있기 때문에 클래스가 바뀌더라도 인터페이스가 바뀌지 않는 한 클라이언트 코드로 변경 내용이 전파되지 않는다.  
두 번째. `클라이언트 코드는 해당 인터페이스를 구현하고 있는 구체적인 클래스가 구현되지 않았더라도 자신의 코드를 개발할 수 있다.`  
테스트 코드를 만들어 해당 인터페이스가 해야 하는 역할에 대해 정의해 주기만 하면 됨.  

다형성에 대해서는 인터페이스의 레퍼런스 변수는 해당 인터페이스를 구현하는 여러 클래스들의 인스턴스를 담을 수 있다. 레퍼런스 변수 안에 어떤 인스턴스가 들어있는지 신경쓰지 않고 메서드를 호출하면 안에 들어 있는 인스턴스 종류에 따라 완전히 다른 동작이 실행될 수 있다.
만약 인터페이스가 필수로 구현해야할 메서드의 집합 역할만 했다면, 인터페이스를 선언해도 추상화했을 때의 이점을 누릴 수 없게 될 것.

## [ArrayList의 크기의 변화](#)
최초 ArrayList 내부에 특정 크기의 배열이 생성된 다음, 용량이 가득 찰 때마다 일정 비율로 늘려간다고 알고 있음. 자바 8 버전 이상을 기준으로 크기를 지정하지 않는 경우 최초 10 크기의 배열이 생성되고 용량이 가득찰 때마다 1.5배씩 늘어가는 것으로 기억함.

## [오버라이딩](#)
부모 클래스에 정의된 메서드를 자식 클래스에서 `재정의`하는 것

## [오버로딩](#)
동일한 메서드의 이름으로 서로 다른 파라미터와 return 타입의 메서드를 여러 개 정의하는 것, `중복적재`

---

## [프로토콜](#)
통신 규약으로 일종의 약속  
사람도 같은 언어를 사용해야 의사소통이 가능한 것처러머, 네트워크 역시 보내는 쪽과 받는 쪽이 서로 동일한 형식과 규칙으로 데이터를 주고 받아야 통신이 가능

## [웹 브라우저에서 www.google.com에 접속하면 어떤 일이 생길까](#)
사용자의 웹 브라우저에서 www.google.com을 입력하면 해당 요청은 DNS에 www.google.com이라는 도메인이 어떤 IP주소를 가지고 있는지 묻는다.  
DNS에서 IP 주소를 알려주면 트래픽이 구글 웹 서버를 향해 출발한다.  
그럼 네트웨크 상의 라우터, 스위치 등 여러 네트워크 장비를 거쳐 구글의 웹 서버에 도달한다.  
구글 웹 서버에서 해당 요청에 맞는 HTML 파일을 응답으로 돌려주면, 그 HTML 파일은 왔던 길을 돌아가서 클라이언트에게 도착해 사용자의 웹 브라우저에 렌더링된다.

### [웹 브라우저에서 URL을 처리하는 과정을 설명, URL이 www.google.com/search?q=url과 같이 되어 있다면?](#)
먼저 브라우저가 URL을 파싱해서 도메인인 www.google.com, 경로인 /search, 쿼리 파라미터인 q=url을 해석합니다.  
그다음 DNS 조회를 통해 도메인을 IP 주소로 변환하고, 서버와 TCP 연결을 맺습니다.  
HTTPS라면 TLS 핸드셰이크로 암호화 통신을 준비한 뒤 GET /search?q=url 형태의 HTTP 요청을 보냅니다.  
서버는 검색 결과 HTML을 응답하고, 브라우저는 HTML, CSS, JavaScript를 파싱하고 렌더링해서 최종 화면을 사용자에게 보여줍니다.

### [만약 DNS가 www.google.com 이라는 도메인에 대한 정보를 가지고 있지 않다면?](#)
만약 DNS가 www.google.com에 대한 정보를 바로 가지고 있지 않다면,
DNS 서버는 직접 최종 답을 아는 서버를 다시 찾아가는 과정을 거치게 됩니다.  
보통 클라이언트는 먼저 로컬 DNS 서버에 질의하고, 로컬 DNS 서버에 캐시가 없으면 루트 DNS 서버에 물어봅니다.  
루트 DNS 서버는 google.com을 관리하는 곳의 정보를 알려주고, 그다음 .com TLD DNS 서버에 질의합니다.  
.com DNS 서버는 다시 google.com의 권한 있는 DNS 서버(Authoritative DNS Server) 정보를 알려줍니다.  
마지막으로 권한 DNS 서버에 질의해서 www.google.com의 실제 IP 주소를 받아옵니다.  
즉, DNS가 처음부터 답을 모르더라도 단계적으로 상위 DNS 서버들을 따라가면서 최종 IP 주소를 찾아오는 구조입니다.

### [트래픽은 사용자의 PC에서 구체적으로 어떤 과정을 거쳐 빠져나가는지 OSI 7계층이나 TCP/IP 4계층을 기준으로 설명](#)
사용자 PC에서 트래픽이 나갈 때는, 애플리케이션에서 만든 데이터가 각 계층을 지나면서 캡슐화됩니다.  
예를 들어 브라우저가 HTTP 요청을 만들면, 전송 계층에서 TCP 헤더가 붙어 세그먼트가 되고, 인터넷 계층에서 IP 헤더가 붙어 패킷이 됩니다.  
이후 네트워크 인터페이스 계층에서 MAC 주소가 포함된 프레임으로 만들어지고, 마지막으로 NIC가 이를 전기 신호나 무선 신호로 변환해서 네트워크로 내보냅니다.  
반대로 수신 측에서는 하위 계층부터 헤더를 제거하면서 원래 데이터로 복원합니다.

### [라우터는 어떻게 구글 웹 서버를 찾아가나요?](#)
라우터는 google.com이라는 도메인 이름을 보고 찾는 것이 아니라, 이미 DNS 조회를 통해 알아낸 목적지 IP 주소를 기준으로 패킷을 전달합니다.  
즉 사용자의 PC가 먼저 DNS를 통해 www.google.com의 IP 주소를 알아내고, 그다음 패킷 안에 출발지 IP와 목적지 IP를 넣어 전송합니다.  
라우터는 이 패킷을 받으면 목적지 IP 주소를 확인하고, 자신이 가지고 있는 라우팅 테이블을 조회해서 그 목적지 대역으로 가기 위한 다음 홉(next hop) 또는 출력 인터페이스를 결정합니다.  
그리고 그다음 라우터로 패킷을 넘기고, 이 과정이 인터넷 구간에서 반복되면서 최종적으로 구글 웹 서버가 속한 네트워크까지 도달하게 됩니다.  
즉, DNS는 목적지 IP를 알아내는 역할, 라우터는 그 IP를 보고 어디로 보낼지 결정하는 역할입니다.


### [구글 웹 서버 장비에서 어떻게 트래픽이 웹 서비스로 접근하나요?](#)
구글 웹 서버 장비에 트래픽이 도착하면, 먼저 패킷은 서버의 NIC(Network Interface Card) 로 들어옵니다.  
NIC는 전기 신호나 광 신호를 디지털 데이터로 바꾸고, 이를 운영체제의 네트워크 스택에 전달합니다.  

그다음 운영체제는 이더넷 프레임의 MAC 헤더를 확인하고, 자기 장비로 온 프레임이면 2계층 헤더를 제거합니다.  
이후 IP 헤더를 확인해서 목적지 IP가 서버 자신인지 검사하고, 맞다면 상위 프로토콜이 TCP인지 UDP인지 확인해서 전송 계층으로 넘깁니다.  

TCP라면 운영체제는 목적지 포트 번호를 보고 어떤 프로세스가 해당 포트를 열어두고 있는지 확인합니다.  
예를 들어 웹 서비스가 80번 포트나 443번 포트에서 listen 중이라면,
그 소켓으로 데이터를 전달합니다.  

그러면 웹 서버 프로세스나 애플리케이션 서버가 HTTP 요청을 읽고, 필요한 비즈니스 로직을 수행한 뒤 응답을 생성합니다.
생성된 응답은 다시 반대로 TCP/IP 스택을 타고 내려가서 패킷으로 만들어져 네트워크로 전송됩니다.  

즉, NIC로 들어온 패킷이 OS 네트워크 스택에서 역캡슐화되고, 포트 번호를 기준으로 웹 서비스 프로세스에 전달되어 처리된다고 정리할 수 있습니다.

### [사용자의 웹 브라우저에 HTML이 렌더링되는 것 외에 다른 과정이 또 있을까요?](#)
브라우저는 서버로부터 HTML을 받은 뒤 단순히 화면에 그리는 것뿐 아니라, 그 안에 포함된 CSS, JavaScript, 이미지, 폰트 같은 추가 리소스도 다시 요청합니다.  

또한 HTML을 파싱해 DOM을 만들고, CSS를 파싱해 CSSOM을 만든 뒤, 이를 결합해서 렌더 트리를 만들고 레이아웃과 페인팅 과정을 수행합니다. 이후 JavaScript가 실행되면서 DOM이 변경될 수 있고, 추가적인 비동기 API 호출(fetch, AJAX) 이 발생할 수도 있습니다.  

즉, 웹 브라우저는 단순히 HTML을 표시하는 것이 아니라 추가 리소스 로딩, 스크립트 실행, 이벤트 처리, 네트워크 재요청, 캐시 활용, 쿠키/세션 처리까지 포함한 여러 과정을 수행합니다.

## [TCP와 UDP의 차이](#)
TCP와 UDP는 둘 다 전송 계층에 속하는 프로토콜입니다.
그러나 트래픽을 전송하는 방식에 있어 두 프로토콜은 큰 차이를 보임.

TCP의 경우 트래픽을 보낼 때 패킷이라는 단위를 사용.  
이 패킷의 전송 과정 전후로 연결을 맺고 끊는 과정이 포함되고, 전송 도중에도 올바르게, 순서대로 도달했는지를 확인하는 과정이 있다.
그래서 패킷 전송시 신뢰성이 더 높다.  

반면 UDP의 경우 트래픽을 보낼 때 데이터그램이라는 단위를 사용하며, 전송 도중에 TCP처럼 확인하는 과정이 없다.  
따라서 상대적으로 TCP보다 신뢰성은 낮지만 속도는 빠르다.

## [공인 IP와 사설 IP의 차이](#)
공인 IP는 전 세계 인터넷 어디에서도 접근 가능한 유일한 주소.
IPv4를 기준으로 사용할 수 있는 주소는 대략 43억 개 정도인데, 인터넷이 연결된 모든 기기에 IP 주소를 부여하기엔 수가 너무 부족합니다.  
그래서 IPv6처럼 더 많은 주소를 할당할 수 있는 방식이 등장했지만 아직 널리 사용되고 있지는 않고, 사설 IP가 IP 부족 문제를 부분적으로나마 해결해주고 있습니다.  
사설 IP는 어떤 네트워크 망 내부에서만 사용되는 IP 주소로, 보통 192.168로 시작하며 우리가 가장 흔히 볼 수 있는 IP이기도 함.  
사설 IP를 사용하는 이유는 공인 IP 할당에 많은 비용이 들어간다는 이유도 있지만, 네트워크 망을 외부와 분리하여 해킹 등의 위험으로부터 상대적으로 안전하게 인프라를 구성하려는 목적도 있습니다.


## [로드 밸런싱](#)
하나의 서버가 받아야 할 트래픽을 여러 서버가 나눠 받도록 부하를 분산하는 것.  
부하 분산을 위해서 L4, L7 스위치 같은 네트워크 장비나 엔진엑스 같은 소프트웨어를 사용하여 서버를 구성합니다.  
이렇게 되면 부하가 분산되는 효과 외에도 스케일 아웃 하기 좋은 환경이 됩니다.
(스케일 아웃: 서버 한 대의 성능을 키우는 게 아니라, 서버 대수를 늘려서 처리량을 늘리는 것)
또한 수평적으로 확장이 가능하기 때문에 들어오는 트래픽이 늘어나는 상황에 대응하기 좋다는 장점이 있습니다.

---

## [스프링 프레임워크와 스프링 부트의 차이](#)
스프링 부트는 스프링 프레임워크를 사용하기 쉽도록 만들어 줍니다.  
스프링 프레임워크는 객체지향적인 개발을 위해 필요한 의존성 주입이나 제어의 역전을 손쉽게 활용할 수 있어 애플리케이션에서 핵심적인 로직에 집중하도록 도와준다.  
스프링 프레임워크는 XML과 같은 여러 가지 초기 설정을 해줘야 하는데, 스프링 부트를 쓰면 초기 설정을 간단하게 할 수 있고 내장 Tomcat을 사용하면 애플리케이션을 손쉽게 빌드하고 배포할 수 있다.

### [객체지향적인 개발이 어떤 것인지 설명](#)
객체지향적인 개발이란 각 객체가 자신의 역할과 책임을 가지도록 설계하는 개발 방식.  
단순히 클래스를 나누는 것이 아니라, 데이터와 기능을 따로 두지 않고 객체가 자신의 상태와 동작을 함께 관리하도록 만드는 것이 중요하다고 생각.  

그래서 한 클래스에 로직이 몰리지 않게 하고, 필요한 책임을 적절히 분리해서 객체들이 서로 협력하도록 구성하는 것이 객체지향적인 개발이라고 생각.  
이렇게 설계하면 요구사항이 바뀌었을 때도 영향을 덜 받게 되고, 유지보수나 확장도 더 쉬워진다고 생각.  
예를 들어 주문 시스템이라면 주문, 할인, 결제 같은 역할을 각각 나눠서, 각 객체가 자기 역할에 맞는 기능을 담당하도록 만드는 방식이 객체지향적인 개발이라고 생각.

### [의존성 주입](#)
객체가 필요한 다른 객체를 직접 생성해서 사용하는 것이 아니라, 외부에서 주입받도록 해서 구체 클래스가 아니라 인터페이스에 의존하게 만드는 방식이라고 생각.  
객체지향에서는 클라이언트 코드가 구현체에 직접 의존하면 한 클래스가 바뀔 때 다른 클래스까지 같이 수정해야 하는 문제가 생길 수 있는데, 의존성 주입을 사용하면 이런 결합을 줄일 수 있습니다.  
예를 들어 클라이언트는 인터페이스만 알고 있고, 실제 어떤 구현체를 사용할지는 외부에서 넣어주기 때문에 구현이 바뀌어도 클라이언트 코드는 크게 수정하지 않아도 됩니다.  
결국 의존성 주입은 추상화와 다형성을 더 잘 활용해서 변경에 유연하고 테스트하기 쉬운 구조를 만드는 방법이라고 생각합니다.

### [제어의 역전](#)
보통 객체가 직접 필요한 객체를 생성하고 관리할 수도 있는데, IoC를 적용하면 이런 제어를 스프링 컨테이너 같은 외부가 대신 맡게 됨.  
그래서 개발자는 객체 생성이나 연결보다는 핵심 로직에 더 집중할 수 있고, 의존성 주입을 통해 객체 간 결합도도 낮출 수 있다고 생각합니다.

### [라이브러리와 프레임워크 차이](#)
라이브러리는 내가 필요할 때 가져다 쓰는 도구이고, 내 코드가 실행 흐름을 제어.  
프레임워크는 애플리케이션의 전체 실행 흐름을 관리하고, 프레임워크가 내 코드를 호출한다.
특정 프레임워크로 개발된 애플리케이션을 다른 프레임워크를 바꾸는 것은 어렵다 왜냐면 실행의 흐름이 해당 프레임워크에 맞춰 개발되어 있기 때문.  
라이브러리는 상대적으로 다른 라이브러리로 바꾸는 것이 간단. 왜냐면 작성한 코드에서만 부분적으로 사용되고 있기 때문

### [maven과 gradle 차이](#)
Maven은 pom.xml과 lifecycle 중심이라 구조가 정형화되어 있고 협업 시 이해하기 쉬운 장점이 있고,  
Gradle은 task 기반에 증분 빌드와 캐시 같은 최적화 기능이 있어서 대형 프로젝트나 멀티모듈 환경에서 더 유연하고 성능상 유리할 수 있다고 생각합니다.  
그래서 단순하고 표준적인 프로젝트면 Maven, 커스터마이징이나 빌드 최적화가 중요하면 Gradle을 선택할 수 있다고 봅니다.  
정리하면 Maven은 표준화와 안정성, Gradle은 유연성과 빌드 최적화 쪽이 더 강합니다.

## [URL과 URI의 차이](#)
URI는 URL보다 상위 개념으로, 리소스를 식별하는 통합 개념입니다.  
URI에는 URL과 URN이 포함됩니다.
URL(Uniform Resource Locator)은 리소스의 위치를 나타내며, scheme, host, port, path 등을 통해 자원에 어떻게 접근할지를 표현합니다.  
URN(Uniform Resource Name)은 리소스의 고유한 이름을 나타내고 책 번호로 사용되는 ISBN이 있습니다.  
즉, URI는 리소스를 식별하는 개념이고, URL은 그중 위치를 이용해 식별하는 방식이며, URN은 이름을 이용해 식별하는 방식입니다.


## [웹 브라우저가 웹 서버와 상호작용 할 수 있는 방법에는 어떤 것들이 있나](#)
URL을 통해 접속하는 방법과 form 태그를 사용하여 상호작용하는 방법이 있습니다. 두 방법은 모두 사용자의 웹 페이지를 새로고침을 해야한다는 한계가 존재합니다.  
그래서 AJAX로 요청하는 방법이 있습니다. AJAX로 요청하면 웹 페이지를 새로고침 하지 않고도 서버와 상호작용 할 수 있습니다.
앞서 이야기한 것들은 클라이언트 쪽에서 서버로 요청을 해야만 데이터를 보낼 수 있었고, 서버에서 클라이언트로 보내는 방법이 필요했고, 그 대안으로 웹 소켓을 통한 연결 정보를 주고 받는 것이 있습니다.

### [웹 소켓](#)
정보 교환을 시작할 수 있는 주체가 오직 '클라이언트'이기 때문에 서버 쪽에서 주도적으로 클라이언트에게 정보를 전달할 수 없는 것.  
웹 소켓은 서버와 클라이언트 간의 통신을 양방향으로 가능하도록 지원한다.  
채팅, 실시간 알림, 주식/코인처럼 계쏙 바뀌는 데이터 표시, 실시간 대시보드, 모니터링 화면

---

### [컴파일 언어와 인터프리터 언어](#)
컴파일 언어는 프로그래밍 언어로 작성된 소스코드가 미리 컴파일되어, 운영체제별로 실행될 수 있는 파일로 배포되는 언어.  
인터프리터 언어는 컴파일 과정 없이 프로그래밍 언어가 코드 실행 환경으로 배포된 후 인터프리터에 의해 실행됨.  

컴파일 언어는 문법적인 오류를 컴파일 시점에 체크하지만, 인터프리터 언어는 실행 시점에 체크한다.

### [JSON과 xml의 차이](#)
JSON은 `{}`와 `"`를 사용해서 키-값 형태로 표현되는 데이터 표현 방식.  
XML은 HTML처럼 태그 단위로 표현되는 방식.  
XMl은 하나의 값을 표현하기 위해 여는 태그와 닫는 태그가 2번씩 들어가야 하기 때문에 일반적으로 JSON보다 데이터의 크기가 커진다.  
XML은 태그 형태로 표현되기 때문에 값에 속성을 부여해 보다 풍부한 데이터 표현이 가능하다.

### [AJAX가 필요한 이유와 AJAX를 사용하면 무엇을 할 수 있나요?](#)
웹 브라우저에서 웹 서버로 새로운 정보를 요청하려면 항상 페이지를 이동해야 했습니다. 그러나 자바스크립트를 사용하여 웹 페이즈를 이동하지 않고 정보를 요청하는 방법이 추가되었고, 그것이 AJAX입니다.  
AJAX를 사용하면 회원가입 페이지를 띄우지 않으면서 아이디 중복 검사를 수행하거나, 인스타그램 같이 피드를 볼 수 있는 페이지에서 스크롤을 내리는 것만으로 다음 목록을 불러올 수 있습니다.

### [동기와 비동기](#)
동기적인 통신은 작업을 요청하는 클라이언트와 작업을 처리하는 서버의 동기화가 필요하기 때문에 클라이언트는 작업을 처리하는 동안 대기하고 있어야 한다.  
비동기적인 통신은 클라이언트가 서버에게 작업을 요청한 후 다른 작업을 처리한다.  
동기적인 방식은 직관적이고 흐름을 따라가기 쉽기 때문에 문제가 생겼을 때 디버깅이 쉽지만 서버가 작업을 처리하는 동안 클라이언트가 대기하게 되면서 다른 작업을 하지 못할 수도 있다.  
비동기적인 방식은 동기적인 방식보다는 직관적이지 않고 디버깅도 어렵지만 작업을 요청해 두고 끝나는 대로 취합하면 전체 작업 속도를 단축할 수 있다.

---

### [서로 다른 애플리케이션끼리 통신할 때 더 효율적인 프로토콜을 사용할 수 있는데, 왜 아직 HTTP를 많이 사용하는 건가요?](#)
HTTP는 대부분의 언어와 라이브러리, 프레임워크에서 기본으로 제공해 주는 클라이언트가 있어 개발이 편함.  
웹 브라우저라는 HTTP 클라이언트가 설치되어 있기 때문에 HTTP가 범용적으로 애플리케이션 간 통신에 사용되고 있다고 생각합니다.

### [HTTP/3에서는 왜 TCP가 아니라 UDP를 사용하나요?](#)
TCP는 연결을 지향하고 손실을 네트워크 레이어에서 감지하는 프로토콜로, 보통 안정성이 높다고 평가됩니다.  
UDP는 연결을 맺지 않고, 전송시 손실된 데이터가 있어도 재전송하지 않는 프로토콜입니다.  
UDP는 아주 단순한 프로토콜이며, 기존에 TCP를 통한 HTTP 통신에서 제공했던 기능은 UDP 위에서 별도로 구현해 주면 된다. TCP와 동일한 기능을 유지하면서 훨씬 가볍고 빠른 프로토콜을 만들 수 있다는 이점이 있기 때문에 HTTP/3에서 UDP를 채택했다고 생각합니다.

### [HTTP 메서드 중 GET과 POST는 각각 어떤 기준으로 구분해 사용하나요?](#)
보통 자원을 조회하는 것에 GET, 자원을 생성하는 것에 POST 메서드를 사용합니다.  
GET 메서드를 사용할 때는 해당 요청이 안전한 요청일 경우로, 해당 요청이 서버의 자원 상태를 변경하지 않는 것을 의미합니다.  
멱등성이 없는 요청인 경우 POST 메서드로 지정하여 사용하곤 합니다.

### [멱등성이란(#)]
한 번 호출한 것도 여러 번 호출한 것의 자원 상태가 동일한 것.  
주로 수정이나 삭제 같은 기능이 멱등성 있는 기능. 
'id가 3인 게시글을 다음 내용으로 수정하라' 던가 'id가 3인 게시글을 삭제하라'와 같은 명령이 한 번이라도 실행되면 특정 내용으로 수정되거나 삭제된 상태가 되었을 것.  
따라서 동일한 명령을 다시 수행한다고 해서 자원의 상태가 달라지지 않고 이런 특성을 멱등성이라고 함.

### [Content Negotiation](#)
서버와 클라이언트 사이에서 어떤 콘텐츠를 제공할지에 대한 협상.  
동일한 URL에 대한 요청이라고 하더라도 어떤 사용자는 영어로된 페이지를 받고 싶고, 다른 사용자는 동일한 URL에 대해 HTML 페이지와 JSON 데이터 중 하나를 선택하여 받고 싶을 수도 있습니다.  
Content Negotiation 과정이 없다면 여러 가지 언어나 데이터 타입별로 페이지를 만들어야 하는데 HTTP는 이런 측면에서 적절한 협상 메커니즘을 가지고 있으며, 이것이 Content Negotiation 입니다.  

구체적으로 Content Negotiation은 클라이언트가 요청 헤더에서 'Accept'로 시작하는 헤더들을 사용하여 어떤 콘텐츠를 받고 싶은지 명시하고, 서버는 최대한 클라이언트가 받고 싶어하는 콘텐츠에 맞춰 주려고 함.

### [HTTP와 HTTPS의 차이는 무엇인가요?](#)
HTTP는 요청과 응답을 평문으로 주고 받고, HTTPS는 요청과 응답을 암호화하여 주고 받습니다.  
데이터를 평문으로 주고받으면 전송 도중에도 데이터의 내용을 알아낼 수 있는데, 비밀번호나 개인정보를 전송할 경우 HTTP를 사용하면 정보가 유출될 위험이 있습니다.  
반면, HTTPS는 데이터를 암호화하기 때문에 데이터 전송 도중 데이터의 내용을 알아낼 수 없어 안전합니다.

---

### [어떤 자원을 저장할 때는 숫자로 된 id를 저장하곤 합니다. 만약 id가 엄청 커진다면 어떤 자료형을 사용하는 것이 적절할까요?](#)
Integer로 약 -21억부터 21억까지 표현이 가능.  
id 값은 일반적으로 양의 정수만 사용하므로 약 21억개의 id를 생성할 수 있습니다.  
서비스를 만들다보면 서비스의 규모에 따라 한 종류의 자원이 21억 개를 넘어서는 상황이 충분히 생길 수 있습니다.  
따라서 더 큰 자료형인 Long을 사용하거나 Long으로도 표현하기에 큰 수라면 UUID를 사용해도 좋을거 같습니다.   

### [레이더드 아키텍처에 대해 설명](#)
애플리케이션을 계층별로 나눠 각 게층에 적절한 책임과 역할을 부여해 계층별 재사용성을 높이는 설계 방법.  
과거에는 Controller-Service-DAO 형태의 3개 계층으로 나눈 3티어 아키텍처가 유행했고, 현재는 표현-응용-도메인-인프라스트렉처로 나눈 4티어 아키텍처가 널리 사용되고 있는 것으로 알고 있습니다.  
이렇게 티어를 나누어 사용하면 디자인 패턴처럼 동일한 구조의 아키텍처에 익숙한 사람이 좀 더 빠르게 프로젝트의 구조를 이해하는데 도움을 줄 수 있다.

### [스레드 safe하다 라는 말의 의미](#)
어떤 코드나 라이브러리가 멀티 스레드 환경에서 적절히 동기화를 처리해 정상적인 동작을 보장하는지를 의미.  
컬렉션 중 스레드 세이프한 자료형이 Vector와 스레드 세이프하지 않은 자료형인 ArrayList가 대표적.  
그렇다면 '항상 스레드 세이프한 컬렉션을 사용해야 하는가?'에 대해서는 해당 컬렉션 인스턴스에 동시에 여러 개의 스레드가 접근할 수 있는지에 따라 달라져야 한다.  
동기화라는 것은 Lock 같은 동기화 매커니즘을 사용하여 성능을 희생하게 되는데, 동기화가 필요하지 않은 곳에 스레드 세이프한 자료형을 사용하면 성능에 손해를 보게되기 때문.

### [애플리케이션을 개발할 때 DTO를 만들어 사용하는 이유](#)
DTO는 게층 간 데이터를 전달하기 위해 사용되는 객체입니다.  
DTO가 없다면 도메인 객체가 그대로 표현 게층까지 와서 클라이언트에게 그대로 노출될 겁니다. 그런 상황에서는 도메인 객체의 필드나 구조를 변경하는 것이 클라이언트에게 노출됩니다.  
그럼 해당 API를 바라보고 있는 클라이언트가 바로 영향을 받게 되지만, DTO를 사용하면 이런 상황을 완화시킬 수 있습니다.  
중간 변환 로직이 있기 때문에 내부적으로 도메인 객체가 변경되어도 DTO 변환 로직만 잘 변경해주면 변경의 여파가 클라이언트에게 미치지 않도록 만들 수 있습니다.  
또한 DTO는 하나의 도메인 객체에 목적별로 여러 개의 DTO를 두어서 서로 다른 API에 대해 서로 다른 DTO를 제공하는 것도 가능하게 만들 수 있습니다.
DTO가 없으면 도메인 객체를 함부로 변경할 수가 없다. 도메인 객체가 변경되면 그 구조 그대로 클라이언트가 보는 API 요청, 응답의 변경으로 이어지기 때문에 DTO를 두면 상대적으로 유연하게 변경할 수 있다.

### [스프링 프레임워크에서는 왜 Bean을 만들고 주입하여 사용할까?](#)
Bean은 스프링 프레임워크에서 관리하는 인스턴스들을 이야기한다.  
스프링 프레임워크는 애플리케이션 시작 시 특정 애너테이션들이 붙어 있는 클래스들을 Bean으로 등록하고, 의존성이 필요한 곳에 해당 Bean을 주입하여 사용합니다.  
이렇게 하면 동일한 인스턴스는 한 번만 생성되고 여기저기서 재사용되기 때문에 인스턴스를 만드는 비용과 메모리를 아낄 수 있습니다.  
또 다른 이점으로 의존성을 주입받아 사용하는 것에서 객체지향적인 이점이 발생합니다.  
테스트 코드를 작성할 때 특정 의존성의 대역 의존성을 주입받아서 사용하고 싶은 경우가 생깁니다. 이런 상황에서 의존성을 주입받아서 사용하는 경우 원래 코드를 수정하지 않고 아주 손쉽게 테스트 할 때 의존성을 주입받아서 사용하고, 실제 애플리케이션이 실행되는 환경에서 동작하는 의존성을 주입받아서 사용할 수 있습니다.

---

---

### [웹 애플리케이션을 개발하면서 데이터를 애플리케이션 위에 저장해도 서비스가 가능할 것 같은데, 왜 데이터베이스를 사용하는지?](#)
저장하려는 데이터가 어떤 것인가에 따라 달라질 것 같습니다.  
애플리케이션 메모리에 저장한 내용은 애플리케이션이 종료되면 날아가 버린다. 애플리케이션이 설치된 서버에 파일 형태로 저장하면 문제를 해결할 수 있겠지만 애플리케이션 간 데이터를 공유하기 어렵다. 애플리케이션끼리 API 통신으로 데이터를 주고받으면 가능할 수 있지만 너무 복잡할 것 같다고 생각.  
따라서 데이터베이스에 데이터를 저장하는 것이 좋겠다고 생각합니다.  
그러면 애플리케이션 간에 데이터를 공유할 필요 없이 저장할 때도 데이터베이스에, 조회할 때도 데이터베이스에서 조회해 오면 간단하기 때문.  
데이터베이스는 이런 데이터 공유 외에도 트랜잭셔널한 처리나 캐시 등 애플리케이션을 저장하고 조회하는데 높은 성능을 제공하는 소프트웨어이므로 사용되고 있다고 생각합니다.  

### [트랜잭션이란](#)
일련의 동작들에 대해 모든 동작이 완전히 실행되거나 완전히 실행되지 않음을 보장하는 것.  
모든 동작이 성공적으로 실행되었다면 해당 트랜잭션을 COMMIT 하여 작업을 확정해야 하고, 특정 동작에 실패하여 해당 트랜잭션이 시작되기 전 상태로 되돌리려 한다면 ROLLBACK 시켜야 합니다.  
이러한 특성이 트랜잭션의 원자성이라는 특징이며, ACID라고 부르는 다른 특징들이 있습니다.

### [DB 커넥션 풀은 왜 사용하나요?](#)
데이터베이스와 통신하기 위해서는 데이터베이스의 커넥션이 필요합니다.  
커넥션 풀을 사용하지 않는다면 매번 통신이 필요할 때 커넥션을 생성했다가 닫는 과정이 필요합니다.  
이것은 데이터베이스와 통신하기 위해 발생하는 오버헤드로, 이런 오버헤드를 줄이기 위해 커넥션을 미리 만들어 두고 ,해당 커넥션을 필요한 곳에 빌려줬다가 사용 후 반납하는 형태로 사용합니다.

### [Thread Pool](#)
데이터베이스와 통신하기 위한 스레드 풀이라는 것이 있습니다.  
스프링 부트 프로젝트는 TOMCAT 이라는 웹 애플리케이션 서버를 사용하는데, 톰캣에서는 기본적으로 클라이언트의 요청을 받기 위해 200개의 스레드를 스레드 풀에 생성해 둔다.  
만약 톰캣이 스레드 풀을 만들어 두지 않고 요청이 올 때마다 스레드를 만들어서 처리했다면, 스레드를 만드는 시간만큼 오버헤드가 발생했을 것.  

### [어떤 DB를 사용해 봤나요?](#)
데이터베이스마다 SQL 문법이 조금씩 다르기 때문에 가급적 표준 SQL 문법에 따라 작성하는 것이 중요합니다.  
가능하다면 애플리케이션에서는 SQL을 직접 사용하지 않고 개발할 수 있는 방법을 사용하면 도움이 됩니다.  예를 들어 JPA 같은 인터페이스를 사용하면 된다고 알고 있습니다. 

### [데이터베이스 비밀번호를 application.properties에 올려두면?](#)
로컬에서 실행하는 데이터베이스라면 문제가 되지 않겠지만, 실제 서비스에 사용되는 데이터베이스의 비밀번호가 올라가는 것은 문제가 될거 같습니다.
비밀번호를 application.properties에 올려더면 그 내용을 깃허브에서 확인 가능하므로 application.properties에 비밀번호를 노출하지 않는 방법이 필요합니다.  
jasypt 같은 라이브러리를 통해 application.properties에 있는 비밀번호를 암호화시키고, 관리해야할 프로젝트 수가 늘어나면 Valut와 같은 솔루션을 사용하는 것이 효율적일 것 같습니다.

---

### [객체지향 프로그래밍에서 이야하는 추상화란?](#)
추상적인 존재에 의존하면서 생기는 유지보수의 용이성이 핵심이라고 생각합니다.  
추상적인 존재에 의존하면 다형성이 있고, 테스트하기 용이한 코드를 작성할 수 있습니다.  
정리하면 객체가 가진 공통된 특징과 행동을 추상화하여 부모 클래스나 인터페이스로 정의하고, 이를 활용해서 구현 클래스를 만드는 것.

### [리팩토링이란?](#)
동일한 입력에 대해 결과의 변경 없이 코드의 구조를 개선하는 것
테스트 코드 없이 리팩토링을 할 수 있지만 테스트 코드가 없이 리팩토링을 한다면 변경전 코드와 동일한 동작을 한다는 확신을 할 수 없으므로 테스트 코드를 작성하고 리팩토링을 진행하는 것이 좋습니다.

### [통합 테스트와 단위 테스트 차이](#)
통합 테스트는 2개 이상의 클래스가 협력하여 특정 기능을 정상적으로 수행하는지 테스트 하는 것.  
2개 이상의 클래스를 진짜 객체로 만들어서 테스트하기 때문에 스프링 프레임워크를 사용하는 경우 스프링 애플리케이션을 실행하여 테스트하게 됨.  
클래스를 Bean으로 만들어서 테스트하기 때문에 테스트를 위해 애플리케이션이 시작되는 시간이 소모됨.  

단위 테스트는 작은 단위로, 보통은 메서드 단위로 테스트를 수행.